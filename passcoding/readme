本工具是自动化加解密工具，支持多种手法的加解密方式，可整体使用，也可在有明确目标的前提下拆分单独使用。
本工具用于解密和加密各种密文，有网站解密和本地解密两种方式。
本地解密用于编码ASCII、base家族（base64/base32/base16）、rot13、AAencode、url解码、js解码、
支持模糊解码，可以输入一种密码家族的统称，自动判断加密方式进行解密，也可只输入密文，程序自动判断加密方式并解密,
在ctf比赛中，每分每秒都很珍贵，但是又有许多加密解密的密文要我们去处理，如果没有提前准备各种密文相应的工具的话，那还要自己现场写。如果遇到一个不熟悉的密文，会让参赛者很紧张，赶紧跑去重新学。
这里我集成了一些ctf中常见的密文处理手段，来帮助参赛者能很快处理密文。它支持明确加密方式的解密，支持半迷糊解密，支持全模糊解密，能够帮助参赛者快速处理熟悉和不熟悉的密文。

在破解无密钥的vigenere时，我们可以对每一行的字母出现频率进行排序，然后根据正常字母频率进行匹配。


uuencode and xxencode need recoding


